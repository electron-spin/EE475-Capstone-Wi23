#include <iostream>
#include <vector>

/**
 * @brief A single coordinate of a landmark of the hand.
 */
//typedef pair<int,int> HandLandmark;

/**
 * @brief An array of pairs of ints of size 5 representing the x and y
 * coordinates of the landmarks of the hand. One pair for each finger.
 * Starting from index 0, the fingers are ordered from thumb to pinky.
 */
//typedef vector<pair<int,int>> HandLandmarks;
